[tool.pytest.ini_options]
addopts = "-ra"
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
target-version = "py312"
line-length = 120
extend-exclude = [
    "notebooks",
]

[tool.bandit]
exclude_dirs = [
    ".git",
    ".venv",
    "venv",
    "tests",
]
