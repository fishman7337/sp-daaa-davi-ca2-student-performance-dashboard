# GitHub Configuration

This folder contains repository automation and GitHub-specific configuration.

The `workflows/` folder defines CI checks for tests, syntax-focused linting, and security scans.
