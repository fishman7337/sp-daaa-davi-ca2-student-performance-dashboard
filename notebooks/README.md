# Notebooks

This folder contains the data cleaning, exploration, and dashboard development notebook.

Current notebook:

- `student-performance-intelligence.ipynb`

The notebook uses `data/raw/` as input and exports cleaned datasets to `data/processed/`.
