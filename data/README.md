# Data

This folder contains assignment datasets used by the notebook and dashboard.

- `raw/` contains the original source workbooks.
- `processed/` contains cleaned workbooks used by `app.py`.

Treat these files as academic data artifacts. Avoid adding unrelated personal data, credentials, or local exports.
